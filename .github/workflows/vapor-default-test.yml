name: vapor-default-test

on:
  pull_request:
    branches: [ master ]
    paths:
      - 'vapor-default/**'

defaults:
  run:
    working-directory: ./vapor-default

jobs:

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php_version: [ 83, 84, 85 ]
    concurrency:
      group: vapor-default-test-${{ github.workflow }}-${{ github.ref }}-${{ matrix.php_version }}
      cancel-in-progress: true
    steps:

    - name: Checkout project
      uses: actions/checkout@v5
      with:
        fetch-depth: 0

    - uses: actions/cache@v4
      with:
        path: vapor-default/vendor/bundle
        key: ${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
        restore-keys: |
          ${{ runner.os }}-gems-

    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.7

    - run: |
        bundle config path vendor/bundle
        bundle install --jobs 4 --retry 3

    - run: |
        bundle exec rubocop
        bundle exec rspec
      env:
        DOCKER_DEFAULT_PLATFORM: ${{ runner.arch == 'ARM64' && 'linux/arm64' || 'linux/amd64' }}
        TARGETARCH: ${{ runner.arch == 'ARM64' && 'arm64' || 'amd64' }}
        __VAPOR_RUNTIME: ${{ runner.arch == 'ARM64' && 'docker-arm' || 'docker' }}
        VAPOR_VERSION: ${{ matrix.php_version }}
        COMPOSER_HASH: c8b085408188070d5f52bcfe4ecfbee5f727afa458b2573b8eaaf77b3419b0bf2768dc67c86944da1544f06fa544fd47
        COMPOSE_DOCKER_CLI_BUILD: 1
        DOCKER_BUILDKIT: 1
